import 'package:flutter/material.dart';

class FeverPage extends StatelessWidget {
  static const String id = 'feverpage';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('cuts'),),
      
      
      //Todo:put components
      
    );
  }
}