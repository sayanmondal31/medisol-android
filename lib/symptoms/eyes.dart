import 'package:flutter/material.dart';

class EyesPage extends StatelessWidget {
  static const String id = 'eyespage';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('cuts'),),
      
      
      //Todo:put components
      
    );
  }
}