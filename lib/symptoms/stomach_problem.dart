import 'package:flutter/material.dart';

class StomachPage extends StatelessWidget {
  static const String id = 'stomachpage';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('cuts'),),
      
      
      //Todo:put components
      
    );
  }
}