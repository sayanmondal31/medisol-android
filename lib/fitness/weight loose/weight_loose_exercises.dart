import 'package:flutter/material.dart';

class WeightLooseExecise extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: <Widget>[
          
        ],
      )
      
    );
  }
}