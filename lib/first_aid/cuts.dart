import 'package:flutter/material.dart';

class CutsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('cuts'),),
      
      
      //Todo:put components
      
    );
  }
}