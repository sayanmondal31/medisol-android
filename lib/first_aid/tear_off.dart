import 'package:flutter/material.dart';

class TearOff extends StatelessWidget {
  static const String id = 'tearoff';
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Tear off'),),
      
      
      //Todo:put components
      
    );
  }
}